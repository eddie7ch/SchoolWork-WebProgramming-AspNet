@model IEnumerable<Book>
@{
    ViewData["Title"] = "Books";
}

<div class="d-flex justify-content-between align-items-center mb-3">
    <h2 class="mb-0">📖 Books</h2>
    <a asp-action="Create" class="btn btn-primary">+ Add Book</a>
</div>

<!-- Search Bar -->
<form asp-action="Index" method="get" class="mb-3">
    <div class="input-group">
        <input type="text" name="searchString" value="@ViewData["CurrentFilter"]"
               class="form-control" placeholder="Search by title, author, ISBN or genre..." />
        <button class="btn btn-outline-primary" type="submit">🔍 Search</button>
        @if (!string.IsNullOrEmpty(ViewData["CurrentFilter"]?.ToString()))
        {
            <a asp-action="Index" class="btn btn-outline-secondary">✕ Clear</a>
        }
    </div>
</form>

@if (!Model.Any())
{
    <div class="alert alert-info">
        @if (!string.IsNullOrEmpty(ViewData["CurrentFilter"]?.ToString()))
        {
            <text>No books match "<strong>@ViewData["CurrentFilter"]</strong>". <a asp-action="Index">Show all books</a>.</text>
        }
        else
        {
            <text>No books found. <a asp-action="Create">Add the first one</a>.</text>
        }
    </div>
}
else
{
    <div class="table-responsive">
        <table class="table table-hover table-bordered align-middle">
            <thead class="table-dark">
                <tr>
                    <th>#</th>
                    <th>Title</th>
                    <th>Author</th>
                    <th>Genre</th>
                    <th>ISBN</th>
                    <th>Year</th>
                    <th>Copies</th>
                    <th>Status</th>
                    <th class="text-center">Actions</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var book in Model)
                {
                    <tr>
                        <td>@book.Id</td>
                        <td><strong>@book.Title</strong></td>
                        <td>@book.Author</td>
                        <td><span class="badge bg-secondary">@(book.Genre ?? "—")</span></td>
                        <td><code>@(book.ISBN ?? "—")</code></td>
                        <td>@(book.PublishedYear?.ToString() ?? "—")</td>
                        <td>@book.AvailableCopies / @book.TotalCopies</td>
                        <td>
                            @if (book.IsAvailable)
                            {
                                <span class="badge bg-success">Available</span>
                            }
                            else
                            {
                                <span class="badge bg-warning text-dark">Fully Borrowed</span>
                            }
                        </td>
                        <td class="text-center">
                            <a asp-action="Details" asp-route-id="@book.Id" class="btn btn-sm btn-outline-secondary">Details</a>
                            <a asp-action="Edit"    asp-route-id="@book.Id" class="btn btn-sm btn-outline-primary">Edit</a>
                            <a asp-action="Delete"  asp-route-id="@book.Id" class="btn btn-sm btn-outline-danger">Delete</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
    <p class="text-muted small">Showing @Model.Count() book(s)</p>
}
